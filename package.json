{
  "name": "gltf2usdz.online",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "bun run --cwd client dev",
    "build": "bun run --cwd client build",
    "deploy": "bun run --cwd client build && bun run --cwd client exec wrangler deploy"
  }
}
